# 投资研究操作系统 - 使用说明书

## 项目概述

投资研究操作系统（investment-os）是一个集成化的投资研究与风控决策平台，整合了市场情报、智能分析、持仓风控和知识复盘四大功能模块。

### 核心功能模块

| 模块 | 子功能 | 说明 | 数据状态 |
|------|--------|------|----------|
| 市场情报 | 财报监控 | SEC财报抓取与分析 | 真实数据 |
| 市场情报 | 关键人物舆情 | X(推特)舆情监控 | 真实数据 |
| 市场情报 | 宏观日历 | 14个核心宏观事件倒计时 | 真实数据 |
| 市场情报 | 央行日历 | FOMC/BOJ/ECB等央行事件 | 真实数据 |
| 市场情报 | 地缘政治 | RSS新闻抓取与分类 | 真实数据 |
| 市场情报 | 大宗商品 | 大宗商品行情 | - |
| 智能分析 | 每日日报 | 基于真实数据生成日报 | 真实数据 |
| 智能分析 | 跨市场联动 | 5大市场指数联动分析 | 真实数据 |
| 智能分析 | AI选股 | 多因子量化选股 | 真实数据 |
| 智能分析 | 技术分析 | 技术指标与形态识别 | 真实数据 |
| 智能分析 | 财报季 | 财报日历与预期 | 半真实 |
| 智能分析 | 关联分析 | 多因子关联分析 | 示例数据 |
| 持仓与风控 | 组合持仓 | 实时市值与盈亏 | 真实数据 |
| 持仓与风控 | 压力测试 | 情景压力测试 | 示例数据 |
| 持仓与风控 | 交易信号 | 量化信号生成 | 示例数据 |
| 持仓与风控 | 告警中心 | 分级告警与推送 | 真实数据 |
| 知识与复盘 | 知识库 | 投研知识管理 | 示例数据 |
| 知识与复盘 | 交易复盘 | 交易记录与复盘 | 示例数据 |
| 知识与复盘 | AI问答 | 智能投研问答 | 示例数据 |
| 知识与复盘 | 策略回测 | 历史回测 | 示例数据 |

---

## 快速开始

### 环境要求

- Python 3.10+
- pip

### 安装依赖

```bash
cd investment-os
pip install -r requirements.txt
```

### 配置

复制 `.env.example` 为 `.env` 并根据需要修改配置：

```bash
cp .env.example .env
```

主要配置项：

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| `OPENBB_BASE_URL` | OpenBB API地址 | `http://159.138.92.82:6900/api/v1` |
| `OPENBB_PROVIDER` | 行情数据提供商 | `yfinance` |
| `PORTFOLIO_DB_PATH` | 数据库路径 | `data/portfolio.db` |
| `PUSHPLUS_TOKEN` | PushPlus推送Token | 空（不推送） |
| `STOP_LOSS_PCT` | 单标的止损线 | `0.15` (15%) |
| `PORTFOLIO_DD_PCT` | 组合浮亏阈值 | `0.10` (10%) |
| `CONCENTRATION_PCT` | 单标的集中度 | `0.30` (30%) |
| `VAR_CONFIDENCE` | VaR置信度 | `0.95` |
| `VAR_LOOKBACK_DAYS` | VaR回看天数 | `60` |

### 启动服务

```bash
# 启动Web服务（默认端口8188）
python server.py --port 8188
```

启动后访问：`http://localhost:8188`

### 启动持仓监控

```bash
# 启动独立的持仓监控进程
python main.py
```

---

## 功能详解

### 1. 市场情报模块

#### 1.1 财报监控
- 自动抓取SEC财报（12家科技公司）
- 点击摘要查看财报详情
- 支持一键推送重要财报到微信
- 数据来源：tesla-sec-fetcher 集成

**API端点：**
- `GET /api/filings` - 获取财报列表
- `GET /api/filings/{symbol}` - 获取指定股票财报详情
- `POST /api/filings/{symbol}/push` - 推送财报到微信

#### 1.2 关键人物舆情
- 实时监控X(推特)关键人物发言
- 自动分级（高/中/低重要性）
- 支持一键推送高级别舆情到微信
- 数据来源：x-monitor-push 数据库

**API端点：**
- `GET /api/sentiment` - 获取舆情列表
- `POST /api/sentiment/push-high` - 推送未读高级别舆情

#### 1.3 宏观日历
- 14个核心宏观事件（CPI/PCE/非农/GDP等）
- 自动计算下次发布时间和倒计时
- 美东时间/北京时间双时区显示
- 数据来源：us-stock-monitor 集成

**API端点：**
- `GET /api/macro/calendar` - 获取宏观日历

#### 1.4 央行日历
- FOMC/BOJ/ECB/BOE等主要央行会议
- 已知会议日期表 + 规则推算双保险
- 数据来源：us-stock-monitor 集成

**API端点：**
- `GET /api/central-bank/calendar` - 获取央行日历

#### 1.5 地缘政治
- 多源RSS新闻抓取（Reuters/CNBC/BBC）
- 自动分类：地缘冲突、经济政策、地缘政治等
- 地区识别：美国、中国、欧洲、中东等
- 影响资产判断：原油、黄金、股市、债券等

**API端点：**
- `GET /api/geopolitics` - 获取地缘新闻（自动同步）
- `POST /api/geopolitics/sync` - 手动触发新闻同步

### 2. 智能分析模块

#### 2.1 每日日报
- 自动聚合当日市场数据
- 内容包括：市场概览、关键事件、持仓变动、明日关注
- 全部基于真实数据生成

**API端点：**
- `GET /api/daily-report` - 获取今日日报

#### 2.2 跨市场联动
- 5大市场指数实时行情：
  - 美股：标普500 (^GSPC)
  - A股：上证指数 (000001.SS)
  - 港股：恒生指数 (^HSI)
  - 欧洲：欧洲斯托克50 (^STOXX50E)
  - 加密：比特币 (BTC-USD)
- 自动计算涨跌幅度
- 联动分析与盘前简报生成

**API端点：**
- `GET /api/cross-market` - 获取跨市场数据

#### 2.3 AI选股
- 20+股票候选池
- 多因子打分：技术形态、动量、RSI、波动率
- 按行业筛选
- 数据来源：OpenBB 历史行情

**API端点：**
- `GET /api/ai-screener` - AI选股筛选
  - 参数：`sector`（行业）、`top_n`（数量）、`min_score`（最低分）

#### 2.4 技术分析
- 实时价格、MA5、MA20
- RSI指标计算
- 技术形态识别（示例）

**API端点：**
- `GET /api/technical/{symbol}` - 技术分析

### 3. 持仓与风控模块

#### 3.1 组合持仓
- 实时行情刷新
- 单标的盈亏计算
- 组合总市值/总盈亏
- 行业集中度分析
- 净值序列与最大回撤

**API端点：**
- `GET /api/portfolio` - 获取持仓组合
- `POST /api/holdings` - 添加持仓
- `DELETE /api/holdings/{symbol}` - 删除持仓
- `POST /api/scan` - 触发风控扫描

#### 3.2 风险指标
- **VaR（风险价值）**：95%置信度下的最大单日损失
- **最大回撤**：基于净值序列的真实回撤
- **集中度风险**：单标的/行业占比过高预警

#### 3.3 告警中心
- 三级告警体系：
  - P0（高）：止损触发、组合大幅回撤
  - P1（中）：集中度超标
  - P2（低）：普通提醒
- 微信推送支持（需配置PushPlus Token）
- 告警冷却机制避免骚扰

**API端点：**
- `GET /api/alerts` - 获取告警列表

### 4. 推送服务

#### 4.1 支持的推送通道

| 通道 | 配置项 | 说明 |
|------|--------|------|
| PushPlus | `PUSHPLUS_TOKEN` | 微信公众号推送，推荐 |
| WxPusher | - | 待实现 |
| Server酱 | - | 待实现 |

#### 4.2 推送特性
- **分级推送**：只有P0/P1级告警才推送
- **冷却机制**：同一类型告警最小间隔60分钟
- **每日上限**：自动控制每日推送数量
- **持久化记录**：SQLite存储推送历史

---

## 部署说明

### 本地运行

```bash
# 安装依赖
pip install -r requirements.txt

# 启动服务
python server.py --port 8188
```

### 服务器部署（Linux + systemd）

项目已包含 `deploy.sh` 一键部署脚本：

```bash
# 上传代码到服务器
scp -r investment-os user@server:/path/to/

# 登录服务器执行部署
ssh user@server
cd /path/to/investment-os
chmod +x deploy.sh
./deploy.sh
```

部署脚本会自动：
1. 创建Python虚拟环境
2. 安装依赖
3. 配置systemd服务
4. 设置开机自启
5. 启动服务

---

## API 完整列表

### 市场情报
| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/filings` | 财报列表 |
| GET | `/api/filings/{symbol}` | 财报详情 |
| POST | `/api/filings/{symbol}/push` | 推送财报 |
| GET | `/api/sentiment` | 舆情列表 |
| POST | `/api/sentiment/push-high` | 推送高级别舆情 |
| GET | `/api/macro/calendar` | 宏观日历 |
| GET | `/api/central-bank/calendar` | 央行日历 |
| GET | `/api/geopolitics` | 地缘新闻 |
| POST | `/api/geopolitics/sync` | 同步新闻 |

### 智能分析
| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/daily-report` | 每日日报 |
| GET | `/api/cross-market` | 跨市场联动 |
| GET | `/api/ai-screener` | AI选股 |
| GET | `/api/technical/{symbol}` | 技术分析 |
| GET | `/api/earnings-season` | 财报季 |
| GET | `/api/correlation` | 关联分析 |

### 持仓与风控
| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/portfolio` | 持仓组合 |
| POST | `/api/holdings` | 添加持仓 |
| DELETE | `/api/holdings/{symbol}` | 删除持仓 |
| POST | `/api/scan` | 风控扫描 |
| GET | `/api/alerts` | 告警列表 |
| GET | `/api/scenario` | 压力测试 |
| GET | `/api/signals` | 交易信号 |

### 知识与复盘
| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/knowledge` | 知识库列表 |
| POST | `/api/knowledge` | 添加知识 |
| GET | `/api/trades` | 交易记录 |
| POST | `/api/trades` | 添加交易 |
| GET | `/api/supply-chain` | 产业链 |

### 系统
| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/api/config` | 配置信息 |
| GET | `/api/push/stats` | 推送统计 |
| GET | `/api/health` | 健康检查 |

---

## 测试

运行全部测试：

```bash
python -m pytest tests/ -v
```

运行指定模块测试：

```bash
# 跨市场测试
python -m pytest tests/test_cross_market.py -v

# 地缘政治测试
python -m pytest tests/test_geopolitics.py -v

# 日报测试
python -m pytest tests/test_daily_report.py -v
```

目前共 **248个测试用例**，覆盖核心功能。

---

## 数据来源说明

| 数据类型 | 来源 | 说明 |
|----------|------|------|
| 股票行情 | OpenBB (yfinance) | 实时报价、历史K线 |
| 宏观日历 | us-stock-monitor | 14个核心宏观事件 |
| 央行日历 | us-stock-monitor | FOMC/BOJ/ECB等 |
| 财报数据 | SEC EDGAR | 12家科技公司财报 |
| 舆情数据 | x-monitor-push | X(推特)舆情 |
| 地缘新闻 | RSS (Reuters/CNBC/BBC) | 自动抓取与分类 |
| 持仓数据 | SQLite | 本地存储 |

---

## 常见问题

### Q: 为什么有些数据显示"获取失败"？
A: 可能是网络问题或数据源暂时不可用。系统会自动降级，不影响其他功能使用。

### Q: 如何开启微信推送？
A: 在 `.env` 中配置 `PUSHPLUS_TOKEN`。注册PushPlus（https://www.pushplus.plus/）后获取Token。

### Q: 如何添加更多股票到持仓？
A: 在"持仓与风控"页面点击"➕ 添加"按钮，输入标的代码、成本价、数量和行业。

### Q: 地缘新闻多久更新一次？
A: 每次访问地缘政治页面时会自动尝试同步最新新闻。也可以通过 `POST /api/geopolitics/sync` 手动触发。

### Q: 如何部署到服务器？
A: 使用项目根目录的 `deploy.sh` 脚本，或参考上面的"部署说明"章节。

---

## 项目文件结构

```
investment-os/
├── server.py              # Web服务主程序（FastAPI）
├── main.py                # 持仓监控主程序
├── config.py              # 配置管理
├── pricing.py             # 行情获取（OpenBB）
├── risk.py                # 风险计算
├── store.py               # 数据库操作（SQLite）
├── alerter.py             # 告警器
├── requirements.txt       # Python依赖
├── deploy.sh              # 部署脚本
├── .env.example           # 配置示例
├── data_sources/          # 数据源适配器
│   ├── sec_filings.py     # 财报数据源
│   ├── macro_calendar.py  # 宏观日历
│   ├── central_bank.py    # 央行日历
│   └── news_fetcher.py    # 新闻抓取
├── shared/                # 共享模块
│   ├── pusher.py          # 统一推送层
│   └── sentiment_adapter.py # 舆情适配器
├── static/                # 前端静态文件
│   ├── index.html         # 主页面
│   └── app.js             # 前端逻辑
├── tests/                 # 测试用例
│   └── test_*.py          # 各模块测试
└── data/                  # 数据目录
    └── portfolio.db       # SQLite数据库
```

---

## 更新日志

### v1.0.0 (2026-07-25)
- 完成20个功能模块整合为5大模块
- 响应式布局，适配手机端和桌面端
- P0级别假数据真实化（4个核心API）：
  - 宏观日历：接入us-stock-monitor
  - 地缘政治：RSS新闻自动抓取与分类
  - 跨市场联动：5大市场指数实时行情
  - 每日日报：基于真实数据自动生成
- 统一推送层（PushPlus三通道框架）
- 248个测试用例，全部通过
